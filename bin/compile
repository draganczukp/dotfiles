#!/bin/zsh

[[ $2 == doc ]] && args="--to=pdf --toc --listings"
[[ $2 == prezentacja ]] && args="-t beamer --slide-level 2"

pushd $PWD
echo $1 | entr pandoc --from=markdown $1 -o ${1:r}.pdf $args

popd
