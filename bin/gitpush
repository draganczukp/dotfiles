#!/bin/zsh
git add .
message=$(curl -s whatthecommit.com/index.txt)
repo='origin'

if [[ $1 != '' ]]; then
	if [[ $1 = '-m' || $1 = 'm' ]]; then
		message=$2
	else
		repo=$1
	fi
fi
git commit -m "$message"
git push $repo
