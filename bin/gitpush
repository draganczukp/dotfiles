#!/bin/zsh
git add .
message=$(curl -s whatthecommit.com/index.txt)
branch='master'

if [[ $1 != '' ]]; then
	if [[ $1 = '-m' || $1 = 'm' ]]; then
		message=$2
	else
		branch=$1
	fi
fi
git commit -m "$message"
git push $branch
