#!/bin/zsh
clear
font="smmono12"
# font="future"
out="";

# neofetch

out+="Witaj, $USER\n";
out+="Jest $(date '+%a, %d %b, %H:%M')\n";
# out+="Cytat: $(fortune -a)"
# out+="$(python ~/bin/weather.py)"

echo $out | toilet -f $font -t
