#!/bin/zsh
clear
# font="smmono12"
font="future"
out="";

neofetch

out+="Hello, $USER\n";
out+="It's $(date '+%a, %d %b, %H:%M')\n";
# out+="Cytat: $(fortune -a)"
# out+="$(python ~/bin/weather.py)"

echo $out | toilet -f $font -t
