#!/bin/zsh
clear
# font="smmono12"
font="future"
out="";

# neofetch

out+="Hello, $USER\n";
out+="It's $(LC_ALL='en_US.UTF-8' date '+%a, %d %b, %H:%M')\n";
# out+="Quote: $(fortune -a)"

echo $out | toilet -f $font -t
# fortune | cowsay -f default
