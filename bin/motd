#!/bin/zsh
clear
# font="smmono12"
font="future"
out="";

# neofetch

out+="Hello, $USER\n";
out+="It's $(LANG=en_US.UTF-8 date '+%a, %d %b, %H:%M')\n";
# out+="Quote: $(fortune -a)"
# out+="$(python ~/bin/weather.py)"

echo $out | toilet -f $font -t
fortune | cowsay -f default
