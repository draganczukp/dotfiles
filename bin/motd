#!/bin/zsh
clear
font="future"
out="";


out+="Hello, $USER\n";
out+="It's $(LC_ALL='en_US.UTF-8' date '+%a, %d %b, %H:%M')\n";

echo $out | toilet -f $font -t

cow=$(find /usr/share/cows -type f -name "*.cow" | sed 's!/usr/share/cows/\(.*\)!\1!g' | shuf -n 1)

fortune -a -s | cowsay -n -f $cow
